<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand fw-bold text-danger" href="/">
      <i class="fa-solid fa-compass"></i> TripNest
    </a>

    <!-- Desktop Search (VISIBLE ONLY ≥ md) -->
    <form
      class="d-none d-md-flex mx-auto"
      role="search"
      action="/listings"
      method="GET"
      style="width: 40%;"
    >
      <input
        class="form-control rounded-pill px-4 shadow-sm border-0 me-2"
        type="search"
        name="q"
        placeholder="🔍 Search destinations..."
      />
      <button class="btn btn-outline-secondary rounded-pill" type="submit">
        Search
      </button>
    </form>

    <!-- Toggle -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible -->
    <div class="collapse navbar-collapse mt-3 mt-md-0" id="navbarNav">

      <!-- Mobile Search (VISIBLE ONLY < md) -->
      <form
        class="d-md-none mb-3"
        role="search"
        action="/listings"
        method="GET"
      >
        <input
          class="form-control rounded-pill px-4 shadow-sm mb-2"
          type="search"
          name="q"
          placeholder="🔍 Search destinations..."
        />
        <button class="btn btn-outline-secondary w-100 rounded-pill">
          Search
        </button>
      </form>

      <!-- Links -->
      <ul class="navbar-nav me-md-auto">
        <li class="nav-item">
          <a class="nav-link" href="/listings">Explore</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/listings/new">Add New Listing</a>
        </li>
      </ul>

      <!-- Auth -->
      <ul class="navbar-nav ms-md-auto">
        <% if (!currUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/signup">Signup</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        <% } %>
      </ul>

    </div>
  </div>
</nav>
